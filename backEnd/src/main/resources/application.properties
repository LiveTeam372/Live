spring.application.name=backEnd
# .env file reading
spring.config.import=optional:file:.env[.properties]
# 실제 데이터베이스 드라이버 클래스를 log4jdbc의 DriverSpy로 설정
spring.datasource.driver-class-name=net.sf.log4jdbc.sql.jdbcapi.DriverSpy
# DataBase URL
spring.datasource.url=jdbc:log4${DATABASE_URL}
# DataBase user ID (developer account)
spring.datasource.username=${DATABASE_USERNAME}
# DataBase Password
spring.datasource.password=${DATABASE_PASSWORD}
# mybatis config
mybatis.configuration.mapUnderscoreToCamelCase=true
# Server
server.port= 16543
# Server auto Restart
spring.devtools.livereload.enabled=true
spring.devtools.restart.enabled=true
# Gmail SMTP 서버 설정
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${spring_mail_username}
spring.mail.password=${spring_mail_password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.transport.protocol=smtp

spring.servlet.multipart.max-file-size=3MB
spring.servlet.multipart.max-request-size=3MB

# 이미지 로컬 연결 (임시) - 프로필 이미지
spring.web.resources.static-locations=file:///D:/Feeljae/workspace/Live/frontEnd/public/images/uploaded/
